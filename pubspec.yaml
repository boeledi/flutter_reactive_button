name: flutter_reactive_button
description: a Widget to mimic the Facebook Reaction button in Flutter and allows re-use in different other use cases than Facebook.
version: 1.0.0
author: Didier Boelens <mail@didierboelens.com>
homepage: https://github.com/boeledi/flutter_reactive_button

environment:
  sdk: ">=1.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.18.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg
